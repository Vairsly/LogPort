# 多服务器 Docker 日志导出工具架构

## 总体方案

基于现有 Flask 原型升级为中央管理式 Web 工具：

- 用户登录后选择服务器、容器和时间范围，提交异步导出任务。
- 后台 Worker 通过 SSH 登录目标服务器，执行受控的 `docker logs` 命令。
- 日志保存到本地数据卷，任务完成后提供下载，24 小时自动清理。
- 使用 Docker Compose 部署 Web 与 Worker，共享 SQLite 数据库和日志目录。
- 前后端保持模块化，方便后续扩展多容器导出、统一登录、PostgreSQL 和审计功能。

## 核心设计

- **界面**
  - 登录页、日志导出页、任务记录页、服务器管理页。
  - 支持“指定开始/结束时间”和“最近 N 分钟/小时”两种输入模式。
  - 时间选择器使用浏览器本地时区，提交时转换为带时区的 ISO 8601。
  - 展示等待、执行中、成功、失败、已过期状态，并自动刷新任务进度。
  - 使用响应式 Bootstrap 风格界面，支持桌面和移动端。

- **服务器管理**
  - 管理员维护服务器名称、地址、SSH 端口、用户名、认证方式和容器白名单。
  - 同时支持 SSH 私钥和密码认证；敏感字段使用环境变量提供的主密钥加密后写入数据库。
  - 提供“测试连接”，检查 SSH、主机身份和 Docker 命令权限。
  - 首次连接必须确认并保存主机指纹；指纹变化时拒绝连接，避免中间人攻击。
  - 容器只能从服务器白名单选择，禁止前端传入任意容器名或命令。

- **异步任务**
  - Web 服务只创建任务；独立 Worker 从 SQLite 原子领取待执行任务。
  - 每个任务限定单服务器、单容器，最大跨度 24 小时。
  - Worker 使用 SSH 执行固定结构的 `docker logs --timestamps --since ... --until ... <container>`。
  - 将标准输出与标准错误合并并流式写入临时文件，完成后原子重命名，避免将全部日志载入内存。
  - 默认单 Worker 串行执行架构，允许以后增加 Worker，并通过任务领取锁避免重复执行。
  - 设置 SSH 连接超时、命令超时和任务取消状态；失败时保存脱敏错误信息。
  - 成功文件按服务器、容器和时间命名，通过受鉴权的下载接口返回。

- **数据与清理**
  - SQLite 保存管理员、服务器、容器白名单、任务状态和审计时间。
  - 日志文件与数据库放在持久化数据卷，不写入容器镜像。
  - 定时清理完成超过 24 小时的日志文件，并将任务标记为已过期。
  - 服务重启后，长时间停留在执行中的任务恢复为失败，避免永久卡住。

## 接口与模块

- `POST /api/login`：管理员登录并建立安全会话。
- `GET/POST/PUT/DELETE /api/servers`：服务器及容器白名单管理。
- `POST /api/servers/{id}/test`：测试 SSH 与 Docker 权限。
- `POST /api/export-tasks`：校验服务器、容器和时间后创建任务。
- `GET /api/export-tasks`、`GET /api/export-tasks/{id}`：查询任务及状态。
- `GET /api/export-tasks/{id}/download`：下载成功且未过期的日志。
- 数据模型包含 `User`、`Server`、`AllowedContainer`、`ExportTask`；SSH 凭据不通过查询接口返回。
- 代码按应用工厂、路由、服务、模型、SSH 客户端、Worker 和清理器拆分，不继续把逻辑集中在单个 `log_server.py`。

## 安全与部署

- 管理员初始账号和密码通过环境变量引导创建，密码使用 Argon2 哈希。
- SSH 凭据加密主密钥、Flask 会话密钥均从环境变量注入，不写入数据库或镜像。
- 所有修改接口启用 CSRF 防护；登录增加限速，会话 Cookie 使用 `HttpOnly`、`SameSite`，HTTPS 下启用 `Secure`。
- SSH 命令参数严格校验和转义，不接受自定义命令。
- 提供 `Dockerfile`、`docker-compose.yml`、示例环境文件、健康检查和部署说明。
- 默认监听内网，由 Nginx/Caddy 提供 HTTPS；不直接暴露公网。

## 测试与验收

- 验证密码与私钥两种 SSH 登录、主机指纹确认及指纹变化拒绝。
- 验证指定时间、最近 N 分钟/小时、跨时区和夏令时输入。
- 拒绝结束时间早于开始时间、未来时间、超过 24 小时和非白名单容器。
- 验证任务状态流转、并发领取、服务重启恢复、SSH 超时和 Docker 命令失败。
- 验证大日志流式写入和下载，不出现明显内存增长。
- 验证未登录下载、跨用户会话访问、路径穿越和命令注入均被阻止。
- 验证日志在 24 小时内可重复下载，过期后文件删除且接口返回明确提示。
- 使用模拟 SSH 单元测试覆盖主要逻辑，并使用一台测试 Docker 主机完成端到端验收。

## 已确定默认项

- 第一版为单管理员账号，不实现角色体系和企业统一登录。
- 使用 Flask、服务端模板与轻量 JavaScript，不引入独立前端工程。
- 使用 SQLite 和本地持久化卷，单机部署；暂不支持多实例 Web 横向扩容。
- 每个任务只导出一台服务器的一个容器，文件格式为原始 `.log`。
- 默认最大时间跨度 24 小时，生成文件保留 24 小时。
